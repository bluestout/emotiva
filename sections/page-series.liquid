<div class="em-series-range-sec">
		<div class="page-width">
			<h3 class="em-series-range-title">{{section.settings.title}}</h3>
            {% for block in section.blocks %}
           {% assign product= all_products[block.settings.product]%}
			<div class="em-series-range-white-blk">
              {% unless forloop.first%}
              <div class="em-series-range-white-cont-logo em-series-range-white-logo-mob">
                  <ul>
                    <li><img src="{{'dtx-logo.jpg'| file_url}}" /></li>
                    <li><img src="{{'dloby-img.jpg' | file_url  }}" /></li>
                  </ul>					
                </div>
              {%endunless%}
				<div class="em-series-range-white-img">
                  {%if block.settings.image %}
                  <img src="{{block.settings.image | img_url: "original"}}" />
			{%else%}
                  <img src="{{ product.featured_image.src | img_url: 'original' }}" />
              {%endif%}
                  </div>
              <div class="em-series-range-white-cont">
					<h3>{{product.title}}</h3>
					<h5>{{block.settings.short}}</h5>
					<p>{{block.settings.long}}</p>
					<div class="em-series-range-cont-prc-rate">
						<div class="em-series-range-cont-price">{{product.price| money}}</div>
                      <span class="stamped-product-reviews-badge" data-product-sku="{{ product.handle }}" data-id="{{ product.id }}" style="display:block;">{{ product.metafields.stamped.badge }}</span>
					</div>
					<div class="em-series-range-cont-btn"><a href="{{product.url}}" class="em-bl-back-btn">Explore</a></div>
				</div>
			</div>
          {%endfor%}
			
			
			<div class="em-series-range-sec-btn"><a href="{{section.settings.link}}" class="em-bl-lined-btn">Explore All Products</a></div>
		</div>
	</div>
{% schema %}
  {
    "name": "page series",
    "settings": [
{
      "type": "text",
      "id": "title",
      "label": {
        "da": "Overskrift",
        "de": "Titel",
        "en": "Heading"
      },
      "default": {
        "da": "Logoliste",
        "de": "Logo-Liste",
        "en": "The X-series Range"
      }
    },
{
      "type": "url",
      "id": "link",
      "label": {
        "da": "Link",
        "de": "Link",
        "en": "Link"
      },
      "info": {
        "da": "Valgfri",
        "de": "Optional",
        "en": "Collection"
      }
    }
]
,
    "blocks": [
      {
        "type": "content",
        "name": "Content",
        "settings": [
          
{
      "id": "product",
      "type": "product",
      "label": {
        "da": "Product",
        "de": "Product",
        "en": "Product"
      }
    },
{
          "type": "image_picker",
          "id": "image",
          "label": {
            "da": "Billede",
            "de": "Foto",
            "en": "Image Optional"
          }
        },
{
      "type": "textarea",
      "id": "short",
      "label": {
        "da": "Overskrift",
        "de": "Titel",
        "en": "Short Description"
      },
      "default": {
        "da": "Logoliste",
        "de": "Logo-Liste",
        "en": "Short Description"
      }
    },
{
      "type": "textarea",
      "id": "long",
      "label": {
        "da": "Overskrift",
        "de": "Titel",
        "en": "Long Description"
      },
      "default": {
        "da": "Logoliste",
        "de": "Logo-Liste",
        "en": "Long Description"
      }
    }
        ]
      }
    ]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
